{
  "name": "syntaxsense",
  "displayName": "SyntaxSense",
  "description": "Learn programming syntax naturally as you code. This extension provides simple explanations for unfamiliar syntax, functions, and patterns directly inside VS Code to make learning new languages easier and less overwhelming.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.120.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "syntaxsense.explainSyntax",
        "title": "Explain Syntax"
      },
       {
    "command": "syntaxsense.showHistory",
    "title": "SyntaxSense: Show History"
  }
    ],

     "keybindings": [
    {
      "command": "syntaxsense.explainSyntax",
      "key": "ctrl+alt+e",
      "mac": "cmd+alt+e",
      "when": "editorTextFocus"
    }
  ],
     "views": {
    "explorer": [
      {
        "type": "webview",
        "id": "syntaxsense.sidebar",
        "name": "SyntaxSense ",
        "icon": "$(book)"
      }
    ]
  }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.120.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.3",
    "eslint": "^9.39.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  }
}
